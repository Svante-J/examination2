const homeView = "/"
const signInView = "/signin"
const profileView = "/profile"
const settingsView = "/settings"
const drinksView = "/drinks"

export default {
    homeView,
    signInView,
    profileView,
    settingsView,
    drinksView
}